# Copy this file to .env and fill in your own values. .env itself is
# gitignored and must never be committed.

# Datafordeler (https://datafordeler.dk) API key used by the
# "Get Buildings and BBR Information" tool (processing/get_buildings_and_bbr_algorithm.py)
# to query the GEODKV and BBR entity-based WFS services.
# Create an IT-system with API-key authentication via Datafordeler's
# Administration portal to obtain one.
DATAFORDELER_API_KEY=
